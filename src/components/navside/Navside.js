import React from 'react';
import styled from 'styled-components';

const StyledNavside = styled.nav`
  position: fixed;
  width: 215px;
  height: 100vh;
`;

const Navside = () => {
  return <StyledNavside></StyledNavside>;
};

export default Navside;
